file (GLOB API_SRC "./api/*.cpp")
file (GLOB CONVERSION_SRC "./conversion/*.cpp")
file (GLOB ARITHMETIC_SRC "./arithmetic/*.cpp")
file (GLOB LOGIC_SRC "./logic/*.cpp")
file (GLOB PERFORMANCE_SRC "./performance/*.cpp")

compile_all("true" "bd" "Internal/block-level/blockdecimal/api" "${API_SRC}")
compile_all("true" "bd" "Internal/block-level/blockdecimal/conversion" "${CONVERSION_SRC}")
compile_all("true" "bd" "Internal/block-level/blockdecimal/logic" "${LOGIC_SRC}")
compile_all("true" "bd" "Internal/block-level/blockdecimal/arithmetic" "${ARITHMETIC_SRC}")
compile_all("true" "bd" "Internal/block-level/blockdecimal/performance" "${PERFORMANCE_SRC}")

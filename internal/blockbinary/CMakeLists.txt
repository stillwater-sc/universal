file (GLOB API_SRC "./api/*.cpp")
file (GLOB CONVERSION_SRC "./conversion/*.cpp")
file (GLOB ARITHMETIC_SRC "./arithmetic/*.cpp")
file (GLOB LOGIC_SRC "./logic/*.cpp")
file (GLOB PERFORMANCE_SRC "./performance/*.cpp")

compile_all("true" "bb" "Internal/block-level/blockbinary/api" "${API_SRC}")
compile_all("true" "bb" "Internal/block-level/blockbinary/conversion" "${CONVERSION_SRC}")
compile_all("true" "bb" "Internal/block-level/blockbinary/logic" "${LOGIC_SRC}")
compile_all("true" "bb" "Internal/block-level/blockbinary/arithmetic" "${ARITHMETIC_SRC}")
compile_all("true" "bb" "Internal/block-level/blockbinary/performance" "${PERFORMANCE_SRC}")

file (GLOB API_SRC "api/*.cpp")
file (GLOB LOGIC_SRC "logic/*.cpp")
file (GLOB CONVERSION_SRC "conversion/*.cpp")
file (GLOB ARITHMETIC_SRC "arithmetic/*.cpp")
file (GLOB STANDARD_SRC "standard/*.cpp")
file (GLOB MATH_SRC "./math/*.cpp")
file (GLOB PERFORMANCE_SRC "./performance/*.cpp")

compile_all("true" "decimal" "Number Systems/static/decimal/api" "${API_SRC}")
compile_all("true" "decimal" "Number Systems/static/decimal/logic" "${LOGIC_SRC}")
compile_all("true" "decimal" "Number Systems/static/decimal/conversion" "${CONVERSION_SRC}")
compile_all("true" "decimal" "Number Systems/static/decimal/arithmetic" "${ARITHMETIC_SRC}")
compile_all("true" "decimal" "Number Systems/static/decimal/standard" "${STANDARD_SRC}")
compile_all("true" "decimal" "Number Systems/static/decimal/math" "${MATH_SRC}")
compile_all("true" "decimal" "Number Systems/static/decimal/performance" "${PERFORMANCE_SRC}")


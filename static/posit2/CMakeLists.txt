file (GLOB API_SRC "./api/*.cpp")
file (GLOB CONVERSION_SRC "./conversion/*.cpp")
file (GLOB LOGIC_SRC "./logic/*.cpp")
file (GLOB ARITHMETIC_SRC "./arithmetic/*.cpp")
file (GLOB MATH_SRC "./math/*.cpp")

compile_all("true" "posit2" "Number Systems/static/floating-point/tapered/posit2/api" "${API_SRC}")
compile_all("true" "posit2" "Number Systems/static/floating-point/tapered/posit2/conversion" "${CONVERSION_SRC}")
compile_all("true" "posit2" "Number Systems/static/floating-point/tapered/posit2/logic" "${LOGIC_SRC}")
compile_all("true" "posit2" "Number Systems/static/floating-point/tapered/posit2/arithmetic" "${ARITHMETIC_SRC}")
compile_all("true" "posit2" "Number Systems/static/floating-point/tapered/posit2/math" "${MATH_SRC}")

file (GLOB ARITHMETIC_SAT_BOTH_SRC "*.cpp")

compile_all("true" "cfloat_ttt" "Number Systems/fixed size/floating-point/linear/cfloat/arithmetic/saturating/subnormal-supernormal" "${ARITHMETIC_SAT_BOTH_SRC}")

file(GLOB API_SRC        "api/*.cpp")
file(GLOB CONVERSION_SRC "conversion/*.cpp")
file(GLOB LOGIC_SRC      "logic/*.cpp")
file(GLOB ARITHMETIC_SRC "arithmetic/*.cpp")
file(GLOB MATH_SRC       "math/*.cpp")

compile_all("true" "binratio" "Number Systems/adaptive/rational/binary/binratio/api"        "${API_SRC}")
compile_all("true" "binratio" "Number Systems/adaptive/rational/binary/binratio/conversion" "${CONVERSION_SRC}")
compile_all("true" "binratio" "Number Systems/adaptive/rational/binary/binratio/logic"      "${LOGIC_SRC}")
compile_all("true" "binratio" "Number Systems/adaptive/rational/binary/binratio/arithmetic" "${ARITHMETIC_SRC}")
compile_all("true" "binratio" "Number Systems/adaptive/rational/binary/binratio/math"       "${MATH_SRC}")
